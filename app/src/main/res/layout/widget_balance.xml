<?xml version="1.0" encoding="utf-8"?>
<!--
    Widget layout for the audio balance widget.
    Uses only views supported by RemoteViews:
    LinearLayout, FrameLayout, TextView, ImageView, Button, ProgressBar.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="12dp"
    android:gravity="center">

    <!-- Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_title"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:textStyle="bold"
        android:gravity="center"
        android:alpha="0.8"
        android:layout_marginBottom="6dp" />

    <!-- Balance value label (tappable → opens config activity) -->
    <TextView
        android:id="@+id/tv_balance_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="C"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="6dp" />

    <!-- Visual balance bar -->
    <ProgressBar
        android:id="@+id/pb_balance"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:max="100"
        android:progress="50"
        android:progressTint="#4FC3F7"
        android:progressBackgroundTint="#37474F"
        android:layout_marginBottom="10dp" />

    <!-- L / Center / R control buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/btn_left"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:text="@string/btn_left"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:background="@drawable/btn_widget_background"
            android:layout_marginEnd="4dp" />

        <Button
            android:id="@+id/btn_center"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:text="@string/btn_center"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:background="@drawable/btn_widget_background"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp" />

        <Button
            android:id="@+id/btn_right"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:text="@string/btn_right"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:background="@drawable/btn_widget_background"
            android:layout_marginStart="4dp" />

    </LinearLayout>

</LinearLayout>
