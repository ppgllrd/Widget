<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        MODIFY_AUDIO_SETTINGS: required to modify audio settings such as volume and balance.
        MODIFY_AUDIO_ROUTING: required to call AudioManager.setMasterBalance() on API 29+.
        This permission is granted only to system or privileged apps; on non-system builds the
        app will fall back to per-stream volume adjustment.
    -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_ROUTING" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BalanceWidget">

        <!-- Widget configuration activity -->
        <activity
            android:name=".BalanceConfigActivity"
            android:exported="true"
            android:label="@string/widget_configure_title"
            android:theme="@style/Theme.BalanceWidget.Configure">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget receiver -->
        <receiver
            android:name=".BalanceWidget"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.balancewidget.ACTION_BALANCE_LEFT" />
                <action android:name="com.example.balancewidget.ACTION_BALANCE_RIGHT" />
                <action android:name="com.example.balancewidget.ACTION_BALANCE_CENTER" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/balance_widget_info" />
        </receiver>

    </application>

</manifest>
